"use client";
import { useEffect,useState } from "react"; import toast from "react-hot-toast"; import api from "@/lib/api"; import { useRouter } from "next/navigation"; import { getSocket } from "@/lib/socket";
export default function FriendsPage(){const [friends,setFriends]=useState([]),[received,setReceived]=useState([]),[sent,setSent]=useState([]);const router=useRouter();const load=async()=>{try{const [f,r,s]=await Promise.all([api.get("/friends"),api.get("/friend-requests"),api.get("/friend-requests?type=sent")]);setFriends(f);setReceived(r.filter(x=>x.status==="pending"));setSent(s.filter(x=>x.status==="pending"))}catch(e){toast.error(e.message)}};useEffect(()=>{const timer=setTimeout(load,0);const s=getSocket();s.on("friendship:updated",load);return()=>{clearTimeout(timer);s.off("friendship:updated",load)}},[]);const respond=async(id,status)=>{try{await api.patch(`/friend-requests/${id}`,{status});toast.success(`Request ${status}.`);load()}catch(e){toast.error(e.message)}};const chat=async(id)=>{try{const c=await api.post("/conversations/direct",{userId:id});router.push(`/chat/conversation/${c.id}`)}catch(e){toast.error(e.message)}};return <main className="mx-auto max-w-5xl p-5 sm:p-8"><h1 className="text-3xl font-bold">Friends</h1><h2 className="mt-7 text-xl font-bold">Requests received</h2>{received.length?received.map(r=><section key={r.id} className="mt-3 flex flex-wrap items-center gap-3 rounded-xl border border-slate-200 p-4"><span><b>{r.sender.displayName}</b> wants to connect</span><button onClick={()=>respond(r.id,"accepted")} className="ml-auto rounded-lg bg-blue-600 px-3 py-1.5 text-white">Accept</button><button onClick={()=>respond(r.id,"rejected")} className="rounded-lg px-3 py-1.5 text-rose-600">Reject</button></section>):<p className="mt-3 text-sm text-slate-500">No pending requests.</p>}<h2 className="mt-8 text-xl font-bold">Your friends</h2>{friends.map(f=><article key={f.id} className="mt-3 flex justify-between rounded-xl border border-slate-200 p-4"><span><b>{f.displayName}</b> <small className="text-slate-500">@{f.username}</small></span><button onClick={()=>chat(f.id)} className="font-semibold text-blue-600">Message</button></article>)}{!friends.length&&<p className="mt-3 text-sm text-slate-500">Accept a request to start chatting.</p>}<h2 className="mt-8 text-xl font-bold">Sent requests</h2><p className="mt-2 text-slate-500">{sent.length ? sent.map((request)=>request.receiver.displayName).join(", ") : "No sent requests pending."}</p></main>}
