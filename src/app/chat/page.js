"use client";
import Link from "next/link"; import { useEffect,useState } from "react"; import toast from "react-hot-toast"; import api from "@/lib/api"; import { getSocket } from "@/lib/socket";
export default function ChatPage(){const [items,setItems]=useState([]),[name,setName]=useState(""),[loading,setLoading]=useState(true);const load=async()=>{try{setLoading(true);setItems((await api.get("/conversations",{params:{limit:100}})).items)}catch(e){toast.error(e.message)}finally{setLoading(false)}};useEffect(()=>{const timer=setTimeout(load,0);const s=getSocket();const refresh=()=>load();s.on("conversation:available",refresh);s.on("message:new",refresh);return()=>{clearTimeout(timer);s.off("conversation:available",refresh);s.off("message:new",refresh)}},[]);const group=async(e)=>{e.preventDefault();try{const c=await api.post("/conversations",{type:"group",name,memberIds:[]});setName("");setItems(x=>[c,...x]);toast.success("Group created.")}catch(e){toast.error(e.message)}};return <main className="mx-auto max-w-5xl p-5 sm:p-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><h1 className="text-3xl font-bold">Conversations</h1><form onSubmit={group} className="flex gap-2"><input required value={name} onChange={e=>setName(e.target.value)} className="min-w-0 flex-1 rounded-xl border border-slate-200 px-3" placeholder="New group name"/><button className="rounded-xl bg-blue-600 px-3 py-2 text-white">Create group</button></form></div>{items.map(c=><Link key={c.id} href={`/chat/conversation/${c.id}`} className="mt-3 block rounded-xl border border-slate-200 p-4 transition hover:bg-slate-50"><b>{c.type==="group"?c.name:"Direct conversation"}</b><p className="truncate text-sm text-slate-500">{c.lastMessage?.content||"No messages yet"}</p></Link>)}{loading?<p className="mt-8 text-slate-500">Loading conversations…</p>:!items.length&&<p className="mt-8 text-slate-500">No conversations yet. Start from Friends or create a group.</p>}</main>}
